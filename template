<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>DermAI - Your Friendly Skin Health Assistant</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 20px;
        }

        .container {
            background: white;
            border-radius: 20px;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
            max-width: 900px;
            width: 100%;
            margin: 0 auto;
            padding: 40px;
            animation: fadeIn 0.5s ease-in;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .header {
            text-align: center;
            margin-bottom: 40px;
        }

        h1 {
            color: #333;
            margin-bottom: 10px;
            font-size: 2.8em;
            font-weight: 700;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .subtitle {
            color: #666;
            margin-bottom: 10px;
            font-size: 1.2em;
            font-weight: 500;
        }

        .welcome-text {
            color: #888;
            font-size: 0.95em;
            margin-top: 10px;
        }

        .upload-area {
            border: 3px dashed #667eea;
            border-radius: 15px;
            padding: 60px 20px;
            text-align: center;
            cursor: pointer;
            transition: all 0.3s ease;
            background: linear-gradient(135deg, #f8f9ff 0%, #f0f2ff 100%);
            margin-bottom: 30px;
        }

        .upload-area:hover {
            border-color: #764ba2;
            background: linear-gradient(135deg, #f0f2ff 0%, #e8ebff 100%);
            transform: scale(1.02);
        }

        .upload-area.dragover {
            border-color: #764ba2;
            background: linear-gradient(135deg, #e8ebff 0%, #dde0ff 100%);
            transform: scale(1.05);
        }

        .upload-icon {
            font-size: 4.5em;
            margin-bottom: 20px;
        }

        .upload-text {
            font-size: 1.3em;
            color: #333;
            margin-bottom: 10px;
            font-weight: 600;
        }

        .upload-hint {
            color: #666;
            font-size: 0.95em;
        }

        #imageInput {
            display: none;
        }

        .preview-section {
            display: none;
            margin-top: 30px;
        }

        .preview-section.show {
            display: block;
            animation: slideDown 0.5s ease-out;
        }

        @keyframes slideDown {
            from {
                opacity: 0;
                transform: translateY(-20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .image-preview {
            width: 100%;
            max-width: 400px;
            margin: 0 auto 30px;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
        }

        .image-preview img {
            width: 100%;
            border-radius: 15px;
            display: block;
        }

        .analyze-btn {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            border: none;
            padding: 18px 50px;
            font-size: 1.2em;
            border-radius: 50px;
            cursor: pointer;
            display: block;
            margin: 0 auto 30px;
            transition: all 0.3s ease;
            font-weight: 600;
            box-shadow: 0 5px 20px rgba(102, 126, 234, 0.4);
        }

        .analyze-btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 25px rgba(102, 126, 234, 0.6);
        }

        .analyze-btn:active {
            transform: translateY(-1px);
        }

        .analyze-btn:disabled {
            opacity: 0.6;
            cursor: not-allowed;
        }

        .results-section {
            display: none;
            margin-top: 30px;
        }

        .results-section.show {
            display: block;
            animation: slideDown 0.5s ease-out;
        }

        .result-card {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 35px;
            border-radius: 15px;
            margin-bottom: 30px;
            box-shadow: 0 10px 30px rgba(102, 126, 234, 0.3);
        }

        .result-title {
            font-size: 1.3em;
            margin-bottom: 15px;
            font-weight: 600;
            opacity: 0.95;
        }

        .result-confidence {
            font-size: 3.5em;
            font-weight: 700;
            margin: 20px 0;
        }

        .result-disease {
            font-size: 2em;
            font-weight: 600;
        }

        .disease-info-section {
            background: #f8f9ff;
            border-radius: 15px;
            padding: 30px;
            margin-bottom: 25px;
            border-left: 5px solid #667eea;
        }

        .disease-info-section h3 {
            color: #333;
            margin-bottom: 15px;
            font-size: 1.4em;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .disease-info-section p {
            color: #555;
            line-height: 1.7;
            font-size: 1.05em;
            margin-bottom: 20px;
        }

        .treatment-section {
            background: #fff9e6;
            border-radius: 15px;
            padding: 30px;
            margin-bottom: 25px;
            border-left: 5px solid #ffc107;
        }

        .treatment-section h3 {
            color: #333;
            margin-bottom: 15px;
            font-size: 1.4em;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .treatment-list {
            list-style: none;
            padding: 0;
        }

        .treatment-list li {
            padding: 12px 0;
            padding-left: 30px;
            color: #555;
            line-height: 1.6;
            font-size: 1.05em;
            position: relative;
            border-bottom: 1px solid rgba(0,0,0,0.05);
        }

        .treatment-list li:last-child {
            border-bottom: none;
        }

        .treatment-list li:before {
            content: "✓";
            position: absolute;
            left: 0;
            color: #ffc107;
            font-weight: bold;
            font-size: 1.2em;
        }

        .consult-section {
            background: linear-gradient(135deg, #fff5f5 0%, #ffe8e8 100%);
            border-radius: 15px;
            padding: 30px;
            margin-bottom: 25px;
            border-left: 5px solid #ff4444;
        }

        .consult-section.urgent {
            background: linear-gradient(135deg, #ffe8e8 0%, #ffd4d4 100%);
            border-left: 5px solid #ff0000;
        }

        .consult-section h3 {
            color: #c33;
            margin-bottom: 15px;
            font-size: 1.4em;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .consult-section.urgent h3 {
            color: #d00;
        }

        .consult-section p {
            color: #555;
            line-height: 1.7;
            font-size: 1.05em;
            font-weight: 500;
        }

        .predictions-list {
            background: #f8f9ff;
            border-radius: 15px;
            padding: 25px;
            max-height: 400px;
            overflow-y: auto;
        }

        .predictions-list h3 {
            color: #333;
            margin-bottom: 20px;
            font-size: 1.3em;
        }

        .prediction-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 15px;
            margin-bottom: 10px;
            background: white;
            border-radius: 10px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
            transition: transform 0.2s ease;
        }

        .prediction-item:hover {
            transform: translateX(5px);
        }

        .prediction-item.top {
            border-left: 4px solid #667eea;
            background: #f0f2ff;
        }

        .prediction-disease {
            font-weight: 600;
            color: #333;
            flex: 1;
        }

        .prediction-confidence {
            font-weight: 700;
            color: #667eea;
            font-size: 1.1em;
        }

        .loading {
            display: none;
            text-align: center;
            padding: 30px;
        }

        .loading.show {
            display: block;
        }

        .spinner {
            border: 4px solid #f3f3f3;
            border-top: 4px solid #667eea;
            border-radius: 50%;
            width: 60px;
            height: 60px;
            animation: spin 1s linear infinite;
            margin: 0 auto 20px;
        }

        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }

        .loading p {
            color: #667eea;
            font-size: 1.1em;
            font-weight: 500;
        }

        .error-message {
            background: #fee;
            color: #c33;
            padding: 20px;
            border-radius: 10px;
            margin-top: 20px;
            border-left: 5px solid #c33;
            display: none;
            font-weight: 500;
        }

        .error-message.show {
            display: block;
        }

        .disclaimer {
            background: #e8f4f8;
            border-left: 5px solid #17a2b8;
            padding: 20px;
            border-radius: 10px;
            margin-top: 30px;
            font-size: 0.95em;
            color: #0c5460;
            line-height: 1.6;
        }

        .disclaimer strong {
            color: #0c5460;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>🔬 DermAI</h1>
            <p class="subtitle">Your Friendly Skin Health Assistant</p>
            <p class="welcome-text">Upload a skin image and get instant AI-powered insights to help guide your next steps</p>
        </div>

        <div class="upload-area" id="uploadArea">
            <div class="upload-icon">📸</div>
            <div class="upload-text">Click to upload or drag and drop</div>
            <div class="upload-hint">Supports JPG, PNG, GIF, BMP (Max 16MB)</div>
            <input type="file" id="imageInput" accept="image/*">
        </div>

        <div class="preview-section" id="previewSection">
            <div class="image-preview">
                <img id="previewImage" src="" alt="Preview">
            </div>
            <button class="analyze-btn" id="analyzeBtn">🔍 Analyze Image</button>
        </div>

        <div class="loading" id="loading">
            <div class="spinner"></div>
            <p>Analyzing your image with AI...</p>
        </div>

        <div class="error-message" id="errorMessage"></div>

        <div class="results-section" id="resultsSection">
            <div class="result-card">
                <div class="result-title">Most Likely Condition</div>
                <div class="result-disease" id="predictedDisease"></div>
                <div class="result-confidence" id="confidence">0%</div>
                <div>Confidence Level</div>
            </div>

            <div class="disease-info-section" id="diseaseInfoSection">
                <h3>📋 About This Condition</h3>
                <p id="diseaseDescription"></p>
            </div>

            <div class="treatment-section" id="treatmentSection">
                <h3>💊 Treatment & Care Tips</h3>
                <ul class="treatment-list" id="treatmentList"></ul>
            </div>

            <div class="consult-section" id="consultSection">
                <h3>👨‍⚕️ When to See a Dermatologist</h3>
                <p id="consultDerm"></p>
            </div>

            <div class="predictions-list">
                <h3>📊 All Predictions</h3>
                <div id="predictionsList"></div>
            </div>
        </div>

        <div class="disclaimer">
            <strong>⚠️ Important Medical Disclaimer:</strong> This tool is for informational purposes only and is not a substitute for professional medical advice, diagnosis, or treatment. Always consult with a qualified healthcare provider or dermatologist for any health concerns. If you have a medical emergency, call emergency services immediately.
        </div>
    </div>

    <script>
        const uploadArea = document.getElementById('uploadArea');
        const imageInput = document.getElementById('imageInput');
        const previewSection = document.getElementById('previewSection');
        const previewImage = document.getElementById('previewImage');
        const analyzeBtn = document.getElementById('analyzeBtn');
        const loading = document.getElementById('loading');
        const errorMessage = document.getElementById('errorMessage');
        const resultsSection = document.getElementById('resultsSection');
        const predictionsList = document.getElementById('predictionsList');
        const diseaseInfoSection = document.getElementById('diseaseInfoSection');
        const treatmentSection = document.getElementById('treatmentSection');
        const consultSection = document.getElementById('consultSection');

        let selectedFile = null;

        // Click to upload
        uploadArea.addEventListener('click', () => {
            imageInput.click();
        });

        // File input change
        imageInput.addEventListener('change', (e) => {
            handleFile(e.target.files[0]);
        });

        // Drag and drop
        uploadArea.addEventListener('dragover', (e) => {
            e.preventDefault();
            uploadArea.classList.add('dragover');
        });

        uploadArea.addEventListener('dragleave', () => {
            uploadArea.classList.remove('dragover');
        });

        uploadArea.addEventListener('drop', (e) => {
            e.preventDefault();
            uploadArea.classList.remove('dragover');
            handleFile(e.dataTransfer.files[0]);
        });

        function handleFile(file) {
            if (!file) return;

            if (!file.type.startsWith('image/')) {
                showError('Please select an image file.');
                return;
            }

            selectedFile = file;
            errorMessage.classList.remove('show');
            resultsSection.classList.remove('show');

            // Show preview
            const reader = new FileReader();
            reader.onload = (e) => {
                previewImage.src = e.target.result;
                previewSection.classList.add('show');
            };
            reader.readAsDataURL(file);
        }

        // Analyze button
        analyzeBtn.addEventListener('click', async () => {
            if (!selectedFile) return;

            loading.classList.add('show');
            previewSection.classList.remove('show');
            resultsSection.classList.remove('show');
            errorMessage.classList.remove('show');
            analyzeBtn.disabled = true;

            const formData = new FormData();
            formData.append('image', selectedFile);

            try {
                const response = await fetch('/predict', {
                    method: 'POST',
                    body: formData
                });

                const data = await response.json();

                if (data.error) {
                    showError(data.error);
                } else if (data.success) {
                    displayResults(data.prediction);
                } else {
                    showError('An unexpected error occurred. Please try again.');
                }
            } catch (error) {
                showError('Failed to analyze image. Please check your connection and try again.');
                console.error('Error:', error);
            } finally {
                loading.classList.remove('show');
                analyzeBtn.disabled = false;
                previewSection.classList.add('show');
            }
        });

        function displayResults(prediction) {
            // Display main prediction
            document.getElementById('predictedDisease').textContent = prediction.predicted_disease;
            document.getElementById('confidence').textContent = prediction.confidence.toFixed(1) + '%';

            // Display disease information
            if (prediction.disease_info) {
                const info = prediction.disease_info;
                
                // Description
                document.getElementById('diseaseDescription').textContent = info.description || 'Information about this condition.';

                // Treatment list
                const treatmentList = document.getElementById('treatmentList');
                treatmentList.innerHTML = '';
                if (info.treatment && info.treatment.length > 0) {
                    info.treatment.forEach(treatment => {
                        const li = document.createElement('li');
                        li.textContent = treatment;
                        treatmentList.appendChild(li);
                    });
                } else {
                    const li = document.createElement('li');
                    li.textContent = 'Consult a dermatologist for treatment options.';
                    treatmentList.appendChild(li);
                }

                // Consult dermatologist
                document.getElementById('consultDerm').textContent = info.consult_derm || 'Please consult a dermatologist for proper diagnosis and treatment.';

                // Set urgency styling
                if (info.urgency === 'high') {
                    consultSection.classList.add('urgent');
                } else {
                    consultSection.classList.remove('urgent');
                }
            }

            // Display all predictions
            predictionsList.innerHTML = '';
            prediction.all_predictions.forEach((item, index) => {
                const div = document.createElement('div');
                div.className = 'prediction-item' + (index === 0 ? ' top' : '');
                div.innerHTML = `
                    <span class="prediction-disease">${item.disease}</span>
                    <span class="prediction-confidence">${item.confidence.toFixed(1)}%</span>
                `;
                predictionsList.appendChild(div);
            });

            resultsSection.classList.add('show');
        }

        function showError(message) {
            errorMessage.textContent = message;
            errorMessage.classList.add('show');
        }
    </script>
</body>
</html>
